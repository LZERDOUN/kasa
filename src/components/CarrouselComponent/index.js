/*import React, { useState } from "react";
import "./style.css";
import bouton-droite from "../../assets/bouton-droite.png";


function Carrousel (props) {
    const [count, setCount] = useState(0);
    return (
    <div className="carrousel">

        <button className="clic-droit" onClick={() => this.setState({ count: this.state.count + 1 })}>
            <img src={} alt="Bouton droite" />
        </button>

        <div className="compteur">
            {count=0 ? `"1/"${props.images.lenght}` : `(${count}+1) "/" ${props.images.lenght}` }
        </div>


      <div className="title" onClick={() => setIsActive(!isActive)}>
        <p>{props.title}</p>
        <button type="button" className="collapsible">
          {isActive ? <FaChevronUp /> : <FaChevronDown />}
        </button>
      </div>
      {isActive && <p className="details">{props.details}</p>}
    </div>
  );
}

export default Carrousel;*/
